# Настройка Telegram уведомлений для сотрудников

## Обзор

Система уведомлений позволяет сотрудникам получать уведомления о событиях через Telegram:
- Создание нового обращения
- Назначение обращения на сотрудника
- Просроченные обращения

## Настройка Telegram бота

### 1. Создание бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

Для получения Chat ID пользователя:

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот вернет ваш Chat ID (например: `123456789`)

### 3. Настройка переменных окружения

Добавьте в `.env.local` (для разработки) или в настройки production окружения:

```bash
# Токен Telegram бота (обязательно)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Chat ID администратора (опционально, для системных уведомлений)
TELEGRAM_ADMIN_CHAT_ID=123456789
```

**Важно:**
- Токен должен храниться только в server-side переменных окружения
- Никогда не добавляйте токен в клиентский код
- Не коммитьте `.env.local` в git

## Настройка уведомлений для пользователя

### Через UI

1. Войдите в систему как сотрудник (chairman, secretary, accountant, admin)
2. Перейдите в `/office/settings/notifications`
3. Включите уведомления
4. Введите свой Telegram Chat ID
5. Выберите типы событий для уведомлений
6. Нажмите "Сохранить"

### Типы уведомлений

- **Новое обращение** — уведомление при создании нового обращения
- **Обращение назначено** — уведомление при назначении обращения на вас
- **Просрочено обращение** — уведомление о просроченных обращениях

## События

### appeal.created

Отправляется при создании нового обращения.

**Параметры:**
- `appealId` — ID обращения
- `title` — заголовок обращения
- `plotNumber` — номер участка (если указан)
- `authorName` — имя автора (если указано)

**Пример сообщения:**
```
🔔 Новое обращение

Уточнение начислений за январь
Участок: Березовая, 12
Автор: Анна Петрова
```

### appeal.assigned

Отправляется при назначении обращения на сотрудника.

**Параметры:**
- `appealId` — ID обращения
- `title` — заголовок обращения
- `assignedTo` — ID пользователя, на которого назначено
- `assigneeRole` — роль назначенного сотрудника

**Пример сообщения:**
```
📌 Обращение назначено

Уточнение начислений за январь
ID: a1
Назначено: user-secretary-default
```

### appeal.overdue

Отправляется при обнаружении просроченного обращения (проверяется при чтении inbox).

**Параметры:**
- `appealId` — ID обращения
- `title` — заголовок обращения
- `plotNumber` — номер участка (если указан)
- `dueAt` — срок выполнения
- `status` — текущий статус

**Пример сообщения:**
```
⚠️ Просрочено обращение

Уточнение начислений за январь
ID: a1
Срок: 20.03.2024
```

## Дайджесты

Дайджесты можно отправлять вручную через API или настроить cron job.

**Пример отправки дайджеста:**
```typescript
import { sendDigest } from "@/server/services/notifications";

// Отправить дайджест для роли secretary
await sendDigest("secretary");
```

## Безопасность

### Требования

1. **Токен бота** должен храниться только в server-side переменных окружения
2. **Chat ID** пользователей хранятся в настройках пользователя (в памяти или БД)
3. Никакие токены не передаются в клиентский код
4. Все API endpoints требуют авторизации

### Проверка безопасности

- ✅ Токен бота читается только из `process.env.TELEGRAM_BOT_TOKEN`
- ✅ Chat ID пользователя хранится в server-side настройках
- ✅ API endpoints проверяют авторизацию
- ✅ Уведомления отправляются только подписанным пользователям

## Troubleshooting

### Уведомления не приходят

1. Проверьте что `TELEGRAM_BOT_TOKEN` установлен в переменных окружения
2. Убедитесь что уведомления включены в настройках пользователя
3. Проверьте что Chat ID указан правильно
4. Проверьте логи сервера на наличие ошибок

### Ошибка "TELEGRAM_BOT_TOKEN not configured"

Убедитесь что переменная окружения установлена:
```bash
echo $TELEGRAM_BOT_TOKEN
```

В production (Vercel):
1. Перейдите в Settings → Environment Variables
2. Добавьте `TELEGRAM_BOT_TOKEN` с токеном бота

### Неверный Chat ID

1. Используйте [@userinfobot](https://t.me/userinfobot) для получения актуального Chat ID
2. Убедитесь что Chat ID — это число (например: `123456789`)
3. Проверьте что бот добавлен в контакты

## API Reference

### GET /api/office/notifications/prefs

Получить настройки уведомлений текущего пользователя.

**Response:**
```json
{
  "prefs": {
    "userId": "user-secretary-default",
    "enabled": true,
    "telegramChatId": "123456789",
    "events": {
      "appealCreated": true,
      "appealAssigned": true,
      "appealOverdue": true
    },
    "updatedAt": "2025-01-01T12:00:00.000Z"
  }
}
```

### POST /api/office/notifications/prefs

Обновить настройки уведомлений.

**Request:**
```json
{
  "enabled": true,
  "telegramChatId": "123456789",
  "events": {
    "appealCreated": true,
    "appealAssigned": true,
    "appealOverdue": false
  }
}
```

**Response:**
```json
{
  "prefs": {
    "userId": "user-secretary-default",
    "enabled": true,
    "telegramChatId": "123456789",
    "events": {
      "appealCreated": true,
      "appealAssigned": true,
      "appealOverdue": false
    },
    "updatedAt": "2025-01-01T12:00:00.000Z"
  }
}
```
