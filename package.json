{
  "name": "snt-ulybka",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:turbo": "next dev --turbopack",
    "dev:e2e": "next dev --webpack",
    "clean": "rm -rf .next .turbo node_modules/.cache",
    "clean:dev": "rm -rf .next .turbo node_modules/.cache && echo '✓ Cleaned build artifacts (preserved data/ and tmp/)' && npm run dev",
    "build": "cross-env NEXT_PUBLIC_BUILD_TIME=$(node -e \"process.stdout.write(new Date().toISOString())\") next build --webpack",
    "build:safe": "bash scripts/safe-build.sh --webpack",
    "build:ci": "rm -f .next/lock && cross-env NEXT_PUBLIC_BUILD_TIME=$(node -e \"process.stdout.write(new Date().toISOString())\") next build --webpack",
    "start": "next start",
    "lint": "eslint",
    "lint:public": "eslint \"app/(public)\"",
    "lint:cabinet": "eslint \"app/(cabinet)/**\" \"app/admin/qa/**\" \"src/lib/qaCabinetStage.*\" \"src/lib/cabinet*\"",
    "typecheck": "tsc --noEmit",
    "typecheck:e2e": "tsc -p tests/e2e/tsconfig.json --noEmit",
    "test": "npm run check:api-contracts && vitest run",
    "test:e2e": "PLAYWRIGHT_BASE_URL=http://localhost:3000 playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:matrix": "PLAYWRIGHT_BASE_URL=http://localhost:3000 playwright test tests/e2e/access-matrix.spec.ts --project=access-matrix",
    "e2e:install": "playwright install --with-deps",
    "e2e:smoke": "wait-on http://localhost:3000 && PLAYWRIGHT_BASE_URL=http://localhost:3000 playwright test --project=smoke-billing-admin --project=smoke-billing-resident",
    "e2e:smoke:ci": "sh -c 'npm run dev -- --port 3000 > /tmp/next-dev.log 2>&1 & DEV_PID=$!; trap \"pkill -P $DEV_PID 2>/dev/null || true; kill $DEV_PID 2>/dev/null || true; wait $DEV_PID 2>/dev/null || true\" EXIT INT TERM; wait-on --timeout 120000 http://localhost:3000; set +e; PLAYWRIGHT_BASE_URL=http://localhost:3000 playwright test --project=smoke-billing-admin --project=smoke-billing-resident; CODE=$?; if [ $CODE -ne 0 ]; then tail -n 200 /tmp/next-dev.log || true; fi; exit $CODE'",
    "e2e:office:ci": "sh -c 'if lsof -i tcp:3000 -sTCP:LISTEN >/dev/null 2>&1; then echo \"Port 3000 already in use. Stop the server or change the port.\"; exit 1; fi; LOG=/tmp/next-dev.log; npm run dev -- --port 3000 > $LOG 2>&1 & DEV_PID=$!; trap \"pkill -P $DEV_PID 2>/dev/null || true; kill $DEV_PID 2>/dev/null || true; wait $DEV_PID 2>/dev/null || true\" EXIT INT TERM; wait-on --timeout 240000 http://localhost:3000; WAIT_CODE=$?; if [ $WAIT_CODE -ne 0 ]; then tail -n 200 $LOG || true; exit $WAIT_CODE; fi; set +e; PLAYWRIGHT_BASE_URL=http://localhost:3000 playwright test --project=smoke-office-admin; CODE=$?; if [ $CODE -ne 0 ]; then tail -n 200 $LOG || true; fi; exit $CODE'",
    "qa:report": "node scripts/qa-report.mjs",
    "check:conflicts": "node scripts/check-conflicts.mjs",
    "db:migrate": "node scripts/db-migrate.ts",
    "deploy": "npm run lint && npm run typecheck && npm run build && node scripts/release.js",
    "deploy:prod": "npm run deploy",
    "deploy:dry": "node scripts/release.js --dry-run",
    "seed:test": "ALLOW_SEED_TEST_DATA=true tsx seed/seed-test.ts",
    "seed:e2e": "ALLOW_SEED_TEST_DATA=true tsx seed/seed-e2e.ts",
    "analyze": "ANALYZE=true next build",
    "perf:budget": "node scripts/check-bundle-size.mjs",
    "check:api-contracts": "node scripts/check-api-contracts.mjs",
    "check:imports": "npm run lint -- --quiet",
    "vercel": "npx vercel@latest",
    "vercel:deploy": "npx vercel@latest deploy",
    "vercel:pull": "npx vercel@latest pull",
    "prepare": "husky"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.34.0",
    "@vercel/analytics": "^1.6.1",
    "@vercel/blob": "^0.27.0",
    "@vercel/postgres": "^0.10.0",
    "@vercel/speed-insights": "^1.3.1",
    "exceljs": "^4.4.0",
    "framer-motion": "^11.5.4",
    "next": "16.1.1",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^16.1.2",
    "@playwright/test": "^1.50.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "cross-env": "^7.0.3",
    "dotenv": "^16.6.1",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.9",
    "tailwindcss": "^4",
    "tsx": "^4.7.1",
    "typescript": "^5",
    "vitest": "^4.0.17",
    "wait-on": "^8.0.1"
  },
  "overrides": {
    "undici": "^6.23.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": "eslint --quiet"
  }
}
